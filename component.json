{
  "name": "imports",
  "repo": "tipm/imports",
  "description": "Expose the component's `require` function to platform's global scope as `imports` and the platform `require` function to components as `require.native`",
  "version": "0.0.1",
  "keywords": [],
  "dependencies": {},
  "development": {},
  "license": "MIT",
  "main": "index.js",
  "scripts": [
    "index.js"
  ]
}